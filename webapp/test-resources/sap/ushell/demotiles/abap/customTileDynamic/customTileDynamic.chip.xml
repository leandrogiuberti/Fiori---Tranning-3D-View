<?xml version="1.0" encoding="UTF-8"?>
<chip xmlns="http://schemas.sap.com/sapui2/services/Chip/1">
    <implementation>
        <sapui5>
            <basePath>./</basePath> <!-- relative to CHIP Definition XML -->
            <!-- namespace cannot be virtual, but is translated to path sap/ushell/demotiles/abap/customTileDynamic  -->
            <viewName>sap.ushell.demotiles.abap.customTileDynamic/DynamicTile.view.js</viewName>
        </sapui5>
    </implementation>
    <appearance>
        <title>Custom Tile Dynamic with standard config structure</title>
        <description>This is a custom tile which uses the same configuration structure as the SAP standard app launcher tiles</description>
    </appearance>
    <contracts>
        <consume id="configuration">
            <parameters>
                <parameter name="tileConfiguration" />
            </parameters>
        </consume>
        <consume id="preview" />
        <consume id="writeConfiguration" />
        <consume id="configurationUi" />
        <consume id="search" />
        <consume id="refresh" />
        <consume id="visible" />
        <consume id="bag" />
        <consume id="url" />
        <consume id="actions" />
        <consume id="types">
            <parameters>
                <parameter name="supportedTypes">tile,link</parameter>
            </parameters>
        </consume>
    </contracts>
</chip>

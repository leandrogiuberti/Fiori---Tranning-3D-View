<mvc:View
    controllerName="sap.ushell.demo.AppDirtyStateProvider.controller.employee.EmployeeList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    <Page id="employeeListPage" showHeader="false" title="{i18n>EmployeeList}"
        showNavButton="true"
        navButtonPress="onNavBack"
        class="sapUiResponsiveContentPadding">
        <content>
            <List id="employeeList" headerText="{i18n>ListOfAllEmployees}" items="{/}">
                <items>
                    <StandardListItem
                        title="{FirstName} {LastName}"
                        iconDensityAware="false"
                        iconInset="false"
                        type="Navigation"/>
                </items>
            </List>
        </content>
    </Page>
</mvc:View>

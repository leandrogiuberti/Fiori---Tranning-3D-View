<core:FragmentDefinition
	xmlns:template="http://schemas.sap.com/sapui5/extension/sap.ui.core.template/1"
	xmlns:core="sap.ui.core"
	xmlns="sap.m"
	xmlns:f="sap.ui.layout.form"
	xmlns:contact="sap.fe.macros.contact"
	template:require="{
				MODEL: 'sap/ui/model/odata/v4/AnnotationHelper'
				}"
>
	<template:if test="{contact>}">
		<VBox class="sapUiSmallMarginTop">
			<contact:CollaborativeHeader
				fullName="{contact>fn@@MODEL.format}"
				role="{contact>role@@MODEL.format}"
				photoSrc="{= ${contact>photo@@MODEL.format} ? ${contact>photo@@MODEL.format} : 'sap-icon://person-placeholder' }"
				mail="{payload>/mail}"
				isNaturalPerson="{payload>/isNaturalPerson}"
			/>

			<contact:TeamContactOptions mail="{payload>/mail}" />

			<f:SimpleForm editable="false" layout="ResponsiveGridLayout" maxContainerCols="1" class="sapMQuickViewPage sapUiSmallMarginTop">
				<core:Fragment fragmentName="sap.fe.macros.contact.ContactQuickViewGroup" type="XML" />
			</f:SimpleForm>
		</VBox>
	</template:if>
</core:FragmentDefinition>

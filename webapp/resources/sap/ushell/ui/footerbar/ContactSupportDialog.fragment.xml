<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core"
        xmlns:form="sap.ui.layout.form">
    <Dialog
        id="ContactSupportDialog"
        title="{i18n>contactSupportBtn}"
        contentWidth="29.6rem"
        horizontalScrolling="false"
        initialFocus="subjectInput"
        class="sapContrastPlus sapUshellContactSupportDialog"
        beforeOpen=".onBeforeOpen"
        afterClose=".onAfterClose">
        <content>
            <form:SimpleForm id="topForm" editable="false" layout="ResponsiveGridLayout">
                <Label id="subjectLabel" required="true" text="{i18n>subjectLabel}"/>
                <Input
                    id="subjectInput"
                    value="{/subject}"
                    valueStateText="{i18n>subjectEmptyErrorMessage}"
                    liveChange=".onInputChange"
                    placeholder="{i18n>subjectPlaceHolderHeader}"/>
                <Label id="textAreaLabel" required="true" text="{i18n>txtAreaLabel}"/>
                <TextArea
                    id="textArea"
                    rows="7"
                    value="{/description}"
                    valueStateText="{i18n>txtAreaEmptyErrorMessage}"
                    liveChange=".onInputChange"
                    placeholder="{i18n>txtAreaPlaceHolderHeader}"/>
            </form:SimpleForm>
            <form:SimpleForm
                id="bottomForm"
                editable="false"
                visible="{= !${/showTechInfo}}"
                ariaLabelledBy="bottomFormLabel">
                <Link id="technicalDataLink" text="{i18n>technicalDataLink}" press=".showTechnicalData"/>
                <core:InvisibleText id="bottomFormLabel" text="{i18n>technicalDataTitle}" />
            </form:SimpleForm>
            <form:SimpleForm
                id="technicalInfoBox"
                editable="false"
                visible="{/showTechInfo}"
                layout="ResponsiveGridLayout"
                class="sapUshellTechnicalInfoBox"
                ariaLabelledBy="technicalInfoBoxLabel">
                <Text text="{i18n>loginDetails}" class="sapUshellContactSupportHeaderInfoText"/>
                <Label text="{i18n>userFld}"/>
                <Text text="{/userName}"/>
                <Label text="{i18n>serverFld}"/>
                <Text text="{/host}"/>
                <Label text="{i18n>eMailFld}" visible="{= !!${/email}}"/>
                <Text text="{/email}" visible="{= !!${/email}}"/>
                <Label text="{i18n>languageFld}"/>
                <Text text="{/language}"/>
                <Text text="" visible="{/showAppData}"/>
                <Text text="{i18n>navigationDataFld}" visible="{/showAppData}" class="sapUshellContactSupportHeaderInfoText"/>
                <Label text="{i18n>hashFld}" visible="{/showAppData}"/>
                <Text text="{/navigationHash}" visible="{/showAppData}"/>
                <Text text="" visible="{/showAppData}"/>
                <Text text="{i18n>applicationInformationFld}" visible="{/showAppData}" class="sapUshellContactSupportHeaderInfoText"/>
                <Label text="{i18n>applicationTypeFld}" visible="{/showAppData}"/>
                <Text text="{/applicationType}" visible="{/showAppData}"/>
                <Label text="{i18n>urlFld}" visible="{/showAppData}"/>
                <Text text="{/url}" visible="{/showAppData}"/>
                <Label text="{i18n>additionalInfoFld}" visible="{/showAppData}"/>
                <Text text="{/additionalInfo}" visible="{/showAppData}"/>
                <core:InvisibleText id="technicalInfoBoxLabel" text="{i18n>technicalDataTitle}" />
            </form:SimpleForm>
        </content>
        <beginButton>
            <Button
                id="contactSupportSendBtn"
                text="{i18n>sendBtn}"
                type="Emphasized"
                press=".onSend"/>
        </beginButton>
        <endButton>
            <Button
                id="contactSupportCancelBtn"
                text="{i18n>cancelBtn}"
                press=".closeDialog"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>
